<%= render "/refinery/admin/make_sortable" %>
<section id='records'>
  <% if @teachers.any? %>
    <%= will_paginate @teachers %>
    <%= render "sortable_list" %>
    <%= will_paginate @teachers %>
  <% else %>
    <p>
      <strong>
        No Items yet
      </strong>
      </p>
  <% end %>
</section>
  <aside id='actions'>
    <ul>
      <li>
        <%= link_to 'Add New Teacher', refinery.new_talks_admin_teacher_path,
       :class => 'add_icon' %>
     </li>
     <li>
       <%= link_to 'Reorder Teachers', refinery.talks_admin_teachers_path,
        :id => "reorder_action", :class => "reorder_icon" %>
       <%= link_to 'Done Reordering',
        refinery.talks_admin_teachers_path, :id => "reorder_action_done",
        :style => "display: none;",
        :class => "reorder_icon" %>
      </li>
    </ul>
  </aside>
