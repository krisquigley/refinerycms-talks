<%= render 'refinery/admin/search_header', :url => refinery.talks_admin_talks_path %>
<div class='pagination_container'>
  <% if @talks.any? %>
    <%= render 'talks' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
