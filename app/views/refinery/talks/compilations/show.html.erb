<div class="row">
  <div class="col-xs-12 site__hero">
  </div>
</div>
<%= render '/refinery/shared/site_hero', object: @page %>
<%= render '/refinery/shared/second_navigation' %>
<div class="col-xs-12 col-sm-6 col-md-6 content__content">
  <h2><%= @compilation.title %></h2>
  <hr/>
  <p class="teacher"><i>by <%= @compilation.teacher.name %></i></p>
  <% if @zip %>
    <div class="sticky-post compilation">
       <p>
        <strong><%= link_to @zip.title, refinery.talks_talk_path(@zip) %></strong>
      </p>
      <p>
        <%= raw(@zip.description) %>
      </p>
    </div>
  <% end %>
  <%= render partial: 'refinery/talks/shared/talk', collection: @talks %>
  <%= will_paginate @talks %>
</div>
</div>
