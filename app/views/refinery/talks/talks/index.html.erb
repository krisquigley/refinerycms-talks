<div class="row">
  <div class="col-xs-12 site__hero">
  </div>
</div>
<%= render '/refinery/shared/site_hero', object: @page %>
<%= render '/refinery/shared/second_navigation' %>
<div class="col-xs-12 col-sm-6 col-md-6 content__content">
  <h2><%= page_title %></h2>
  <hr/>
  <%= raw(@page.content_for(:body)) %>
  <div class="row">
  <div class="col-xs-12 col-md-6">
    <h3>teachers</h3>
    <hr/>
    <% @teachers.each do |teacher| %>
      <p><%= link_to teacher.name, refinery.talks_teacher_path(teacher) %></p>
    <% end %>
  </div>
  <div class="col-xs-12 col-md-6">
    <% if @guest_teachers.any? %>
      <h3>guest teachers</h3>
      <hr/>
      <% @guest_teachers.each do |teacher| %>
      <p><%= link_to teacher.name, refinery.talks_teacher_path(teacher) %></p>
      <% end %>
    <% end %>
  </div>
</div>
  <h3>recent talks</h3>
  <hr/>
  <%= render partial: 'refinery/talks/shared/talk', collection: @talks %>
  <%= will_paginate @talks %>
</div>
</div>
